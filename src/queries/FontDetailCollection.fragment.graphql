fragment FontDetailCollection on FontCollection {
  id
  name
  collectionType
  isVariableFont
  cssUrl
  languages(separator: COMMA)
  sku {
    id
  }
  bundles {
    sku {
      id
    }
  }
  fontStyles {
    name
    keyCharacters
    cssFamily
    cssWeight
    cssStyle
    webfontSources {
      url
      format
    }
    sku {
      id
    }
    variableInstances {
      name
      coordinates {
        axis
        value
      }
    }
  }
}
