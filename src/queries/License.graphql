query License($slug: String!) {
  viewer {
    slug(name: $slug) {
      license {
        id
        name
        text(format: HTML)
        pdf {
          url
        }
      }
    }
    licenses {
      id
      name
      slug { name }
      pdf {
        url
      }
    }
  }
}
