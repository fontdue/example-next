#import "./FontDetail.fragment.graphql"

query Font($slug: String!) {
  viewer {
    slug(name: $slug) {
      fontCollection {
        ...FontDetail

        pageMetadata {
          title
          description
          keywords
        }
      }
    }
  }
}
